enum ScheduleFrequency {
  WEEKLY
  MONTHLY
  YEARLY
  CUSTOM
}

enum GatewayEnvironment {
  SANDBOX
  PRODUCTION
}

enum TransactionStatus {
  PENDING
  SUCCESS
  FAILED
}

enum RefundStatus {
  PENDING
  APPROVED
  REJECTED
  PROCESSED
}

enum InvoiceStatus {
  UNPAID
  PAID
  CANCELLED
}

enum SettlementStatus {
  PENDING
  COMPLETED
  FAILED
}
